{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Throne: Standalone",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev:standalone",
      "cwd": "${workspaceFolder}"
    }
  ]
}
